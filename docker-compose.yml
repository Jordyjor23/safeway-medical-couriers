services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: safeway
      POSTGRES_PASSWORD: safeway
      POSTGRES_DB: safeway
    ports:
      - "5432:5432"
    volumes:
      - safeway_pg:/var/lib/postgresql/data

volumes:
  safeway_pg:
